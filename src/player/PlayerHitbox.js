

export default class PlayerHitbox {
    constructor(player, size) {
        this.player = player;
        this.width = size;
        this.height = size;
    }

    setPosition(x, y) {
        
    }
}